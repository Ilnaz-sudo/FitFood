#:import NoTransition kivy.uix.screenmanager.NoTransition
<Profile>:
    FloatLayout:

        MDLabel:
            text:"Здесь будет профиль"
            halign: 'center'
            size_hint_y: None
            pos_hint:{"top": 0.7}

        FloatLayout:
            orientation: "horizontal"
            MDIconButton:
                icon: "cube-scan"
                width: root.width/3
                pos_hint:{"center_x": 0.1, "top":0.1}
                on_press:
                    app.root.transition=NoTransition()
                    root.manager.current = 'camera'


            MDIconButton:
                icon: "home"
                width: root.width/3
                pos_hint:{"center_x": 0.5, "top":0.1}
                on_press:
                    app.root.transition=NoTransition()
                    root.manager.current = 'menu'

            MDIconButton:
                icon: "tooltip-account"
                width: root.width/3
                pos_hint:{"center_x": 0.9, "top":0.12}